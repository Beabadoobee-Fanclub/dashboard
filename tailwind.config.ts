import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{html,js,svelte,ts}"], // adjust as needed
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
